  <section>
    <div class="container mt-5">

      <div class="row">
        {{#each products}}

        <div class="col-md-3">
          <div class="card" style="width: 18rem;">  
            <div class="card-body">
              <h5 class="card-title">{{this.name}}</h5>
              <p class="card-text">{{this.description}}</p>
                    <img src="/product-images/{{this._id}}.jpg" height="100px",width50px></img>

            </div>
          </div>
        </div>
        {{/each}}
      </div>
    </div>
    </div>
  </section>